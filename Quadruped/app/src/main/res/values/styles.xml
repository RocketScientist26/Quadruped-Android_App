<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:splitMotionEvents">false</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Spinner -->
    <style name="connect_SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:paddingLeft">8dp</item>
        <item name="android:background">@null</item>
        <item name="android:popupBackground">@null</item>
        <item name="android:dropDownSelector">@null</item>
        <item name="android:colorPressedHighlight">@null</item>
    </style>

</resources>
